<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Obscura_Client.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Obscura_Client"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Obscura VPN">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar
            x:Name="AppTitleBar"
            Title="Obscura"
            IsBackButtonVisible="False"
            IsPaneToggleButtonVisible="False"
            PaneToggleRequested="TitleBar_PaneToggleRequested">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/Square150x150Logo.scale-200.png" />
            </TitleBar.IconSource>
        </TitleBar>

        <NavigationView
            x:Name="NavView"
            Grid.Row="1"
            IsBackButtonVisible="Collapsed"
            IsPaneToggleButtonVisible="False"
            IsSettingsVisible="False"
            IsPaneVisible="False"
            SelectionChanged="NavView_SelectionChanged"
            PaneDisplayMode="Auto"
            CompactModeThresholdWidth="548"
            ExpandedModeThresholdWidth="671"
            OpenPaneLength="170">

            <NavigationView.MenuItems>
                <NavigationViewItem Content="Connection">
                    <NavigationViewItem.Tag>
                        <local:NavigationView>Connection</local:NavigationView>
                    </NavigationViewItem.Tag>
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Globe" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Location">
                    <NavigationViewItem.Tag>
                        <local:NavigationView>Location</local:NavigationView>
                    </NavigationViewItem.Tag>
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="MapPin" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Help">
                    <NavigationViewItem.Tag>
                        <local:NavigationView>Help</local:NavigationView>
                    </NavigationViewItem.Tag>
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Help" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="About">
                    <NavigationViewItem.Tag>
                        <local:NavigationView>About</local:NavigationView>
                    </NavigationViewItem.Tag>
                    <NavigationViewItem.Icon>
                        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE946;"/>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Settings">
                    <NavigationViewItem.Tag>
                        <local:NavigationView>Settings</local:NavigationView>
                    </NavigationViewItem.Tag>
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Setting" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem
                    x:Name="DeveloperNavItem"
                    Content="Developer"
                    Visibility="Collapsed">
                    <NavigationViewItem.Tag>
                        <local:NavigationView>Developer</local:NavigationView>
                    </NavigationViewItem.Tag>
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Repair" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem Content="Account">
                    <NavigationViewItem.Tag>
                        <local:NavigationView>Account</local:NavigationView>
                    </NavigationViewItem.Tag>
                    <NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Contact" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <Grid>
                <WebView2
                    x:Name="WebView"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch" />
                <Grid
                    x:Name="ErrorFallback"
                    Background="{ThemeResource SolidBackgroundFillColorBaseBrush}"
                    Padding="32"
                    Visibility="{x:Bind NativeUiErrorVisibility, Mode=OneWay}">
                    <StackPanel
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        MaxWidth="560"
                        Spacing="16">
                        <FontIcon
                            FontFamily="Segoe MDL2 Assets"
                            Glyph="&#xE783;"
                            FontSize="40"
                            Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                            HorizontalAlignment="Center" />
                        <TextBlock
                            Text="Native UI Error"
                            Style="{ThemeResource TitleTextBlockStyle}"
                            HorizontalAlignment="Center" />
                        <TextBlock
                            Text="Please report this issue to us and include a debug bundle."
                            Style="{ThemeResource BodyTextBlockStyle}"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            TextWrapping="Wrap"
                            TextAlignment="Center" />
                        <HyperlinkButton
                            Content="support@obscura.net"
                            NavigateUri="mailto:support@obscura.net"
                            HorizontalAlignment="Center" />
                        <Border
                            Background="{ThemeResource LayerFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="12"
                            MaxHeight="200">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{x:Bind NativeUiErrors, Mode=OneWay}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="local:NativeUiError">
                                            <TextBlock
                                                Text="{x:Bind Message}"
                                                FontFamily="Consolas"
                                                FontSize="12"
                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                TextWrapping="Wrap"
                                                IsTextSelectionEnabled="True"
                                                Margin="0,0,0,8" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>
                        <Button
                            Content="Dismiss"
                            HorizontalAlignment="Center"
                            Visibility="{x:Bind NativeUiErrorDismissVisibility, Mode=OneWay}"
                            Click="OnDismissErrorsClick" />
                    </StackPanel>
                </Grid>
            </Grid>
        </NavigationView>
    </Grid>
</Window>
